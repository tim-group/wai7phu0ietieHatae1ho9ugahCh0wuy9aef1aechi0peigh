apply plugin: 'java'
apply plugin: 'eclipse'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'com.rabbitmq', name: 'amqp-client', version: '3.1.3'

    testCompile group: 'junit', name: 'junit', version: '4.11'
}

eclipse {
    classpath {
        defaultOutputDir = file('.eclipse')
    }
}
